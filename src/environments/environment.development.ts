export const environment = {
  production: false,
  /*
   * I will send the needed key, since it's not good to publish it.
   * It would be more elegant with an .env, and I tried it, but it's a lot of effort for this small project.
   */
  API_KEY: 'ADD_API_KEY_HERE',
};
